export const meta = {
  title: "About",
  slug: "/about",
};

# About

This site was incrementally generated, using the develop command!
No need to rerun the builder on each pass through

I haven't figured out how to make it hot reload yet though

```c
#include stdio.h

int main() {
  printf("Hello, world!");
  return 0
}
```